export function SettingsPage() {
  return (
    <section className="pageStack">
      <div>
        <p className="eyebrow">Admin settings</p>
        <h1>Runtime Configuration</h1>
        <p className="lede">
          Configuration CRUD pages will manage provider, model, search, and scoring settings.
        </p>
      </div>
    </section>
  );
}
